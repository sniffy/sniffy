# Ubuntu slim capability experiment summary

The existing runner switch is replaced by an evidence-first probe. Production remains on `ubuntu-24.04` until a real slim job
shows that the publication toolchain is available without costly recurring installation.