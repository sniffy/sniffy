# Status checkout optimization summary

The workflow keeps the existing runners and behavior while limiting checkout to the files each job actually consumes. Runner
migration remains a separate experiment so performance and compatibility results are attributable.