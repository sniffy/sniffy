# Local Codex prompts

The canonical Scheduled-task prompt is [`scheduled-task-prompt.md`](scheduled-task-prompt.md).

`docs/local-codex-worker.md` should describe how to configure the worker and link to the canonical prompt instead of duplicating it.
